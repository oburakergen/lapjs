import localStorage from './local/index';
import cookie from './cookie/index';
import session from './session/index';
import indexedDB from './indexedDB/index';

export default {
    localStorage,
    cookie,
    session,
    indexedDB
};
